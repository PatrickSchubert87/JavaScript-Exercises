/**
 * Review the file named Exercise.html in the Workspace below.
Wrap setTimeout in a Promise inside the wait() function. resolve() in setTimeout's callback.
console.log(this) inside the Promise and observe the results.
Make sure wait() returns the Promise too!
When complete, feel free to check out our implementation in the Solution.html file.
 */